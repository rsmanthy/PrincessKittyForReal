using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ship
{
    // ##  Visual parameters ##
    public float size;

    // ##  Functional parameters ##
    // Speed in which ship goes to asteroids
    public float speed;
    // Amount of ores ship can hold
    public float capacity;
    // Quality of materials ship can hold, 1 being lowest "tier"
    public int drillTier;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
