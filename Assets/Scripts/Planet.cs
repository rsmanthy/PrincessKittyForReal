using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Planet
{
    // ## Visual Parameters ##

    // ##  Functional parameters ##
    // Multiply money by refinement multiplier
    public float refinementMultiplier;
    // Max amount of ships planet can send out at any point in time
    public int shipCount;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
